# Containers project instructions

- Read `SPEC.md` before changing either application snapshot.
- The example application is named Hero Association.
- The `initial` application is stateless and must not persist hero data.
- The `final` application uses PostgreSQL, Docker Compose, and a multi-stage Dockerfile.
- Keep `initial` and `final` deliberately separate so each remains a runnable checkpoint.
